-- tacos/adminlte assets script (for mlua)
-- 若需要可在此放置下載流程（curl/wget），目前保持無副作用。
return {
  framework = "adminlte",
  status = "ready",
  note = "place static assets under templates/adminlte/assets when needed"
}

